This was vibe-coded in a night to
1. Get a working timer app for personal use
2. Go through the process of vibe-coding.

The process was over back-and-forth prompting over several sessions on opencode + gemma4b:26B on a local ollama instance. The model was prompted to make a plan for this. It started with a prototype to fill out the UX from a starting point. It then filled out functionality. User had to correct code that would be incorrectly deleted by the agent, and had to clear out context between high-level implementation tasks.
